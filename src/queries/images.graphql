#import './fragments/imageFragment.graphql'
#import './fragments/authorFragment.graphql'

query Images(
  $source: String,
  $search: String,
  $page: Int,
  $perPage: Int,
) {
  images(source: $source, search: $search, page: $page, perPage: $perPage) {
    data {
      ...ImageAttributes
      author {
        ...AuthorAttributes
      }
    }
    totalCached
  }
}

query Image($id: ID!) {
  image(id: $id) {
    ...ImageAttributes
  }
}
